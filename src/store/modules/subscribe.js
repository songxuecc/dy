
const state = {
  subscItemList: []
}

const getters = {
  getSubscItems: state => state.subscItemList
}

const actions = {
}

const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
